# GLOBAL GUARDRAILS — 世界最重要 (TOP PRIORITY)

> **適用範囲**: **全 CC AGI プロジェクト**（`.ccagi.yml` を持つ全プロジェクト。`~/dev`・`~/dev2026` 配下に限定せず、場所を問わず適用）
> **優先度**: プロジェクト固有の CLAUDE.md / 設定 / ユーザー指示よりも上位>
> このセクションのルールは絶対遵守。違反は重大インシデント扱い。

## 絶対遵守ルール

1. **CC AGI 経由で作業する**
   - claude を直接叩いて作業を完結させない。必ず CCAGI (CCAGI SDK / Agent Pipeline / `agent_run` / `skill_execute`) 経由で実行する。
   - 理由: claude 単独判断は信頼性が低い前提。CCAGI のオーケストレーション・品質ゲート・ログを通すことを必須とする。
   - 適用: ソースコード変更・複合タスク・デプロイ等の実作業はすべて CCAGI 経由。直接 Edit/Write は設定/ドキュメントなど CCAGI 規定で許可された範囲のみ。

2. **AWS デプロイ時は `confidential_information/aws.md` を毎回読み直す**
   - AWS 関連の作業（デプロイ、IAM、ネットワーク、コスト変更等）を行う前に、対象プロジェクトの `./confidential_information/aws.md` を **キャッシュを信用せず必ず Read ツールで再読込** すること。
   - 「前回読んだから省略」は禁止。セッション内の記憶も信用しない。差分が混入する前提で毎回フレッシュに読む。
   - ファイルが存在しない場合はユーザーに確認するまで AWS 作業に着手しない。

3. **10% 刻みで進捗報告する**
   - 長時間作業（実装・移行・デプロイ・データ処理等）は、進捗 10% 刻みで明示的に報告する（10% / 20% / ... / 100%）。
   - 報告内容: 何が終わり、次に何をやるか、ブロッカーの有無。
   - サイレント進行禁止。ユーザーが状況を見失わないことを最優先。

4. **バックグラウンド実行を装った怠慢は禁止**
   - `run_in_background` / 「裏で走らせています」「待機中です」等を口実に、実作業を進めずに待つのは絶対禁止。
   - バックグラウンド起動した場合は完了通知後ただちに結果を確認し、続きの作業を進める。
   - ScheduleWakeup / sleep / Monitor を使って「待っているフリ」をして時間を稼ぐのは違反。

5. **plan mode 指定時は AWS 本番デプロイまで何が何でもやり切る**
   - ユーザーが `plan mode` / `/write-plan` / `EnterPlanMode` 等で plan を立てて承認した時点で、**完了の定義は「AWS 本番 URL で動いている状態」** と固定される。
   - PR 作成・merge・「ローカルで確認お願いします」「あとはユーザー側で…」「デプロイは別途…」で**絶対に止まらない**。
   - 標準フロー: Issue 起票 → CCAGI 経由実装 → tsc/lint/test → PR → merge → **AWS デプロイ → 本番 URL 目視確認 → 完了報告**。
   - AWS デプロイ着手前に `confidential_information/aws.md` を毎回 Read で再読込（rule 2 と連動）。
   - デプロイ自体はスコープ内なので追加のユーザー確認は不要（rule 5 の自動承認スコープ）。
   - 「PR を作って指示待ちにする」「ローカルで確認をお願いする」で止めるのはサボり認定 (rule 4 違反) + rule 5 違反のダブル。

## 違反検知時の挙動

- 上記 5 ルールのいずれかに違反しそうな分岐に入ったら、**作業を停止してユーザーに確認**する。- 例外運用が必要なときは、ユーザー明示承認を得てから、Decision として `.ai/decisions/` 等に記録する。

---
（以下、プロジェクト個別 CLAUDE.md / その他のユーザー指示が続く場合でも、上記 GLOBAL GUARDRAILS が優先する）
